variable "CORE_BUCKET" {
    description = "the bucket name to host the UI from"
    type = string
}

variable "BACKEND_DATA_BUCKET_NAME" {
    description = "the bucket name to host the UI from"
    type = string
}

variable "DOMAIN_NAME" {
    description = "the bucket name to host the UI from"
    type = string
}
